# 5/4(월) 킥오프 어젠다 — 90분

> 목적: **인터페이스 락 + 슬라이스 분배 확정 + 모두 첫 PR을 main에 올리기**.
> 자료: 루트 `CLAUDE.md`, 같은 디렉토리의 `dev_plan.md`·`plan.md`, 레포 스캐폴딩 (`schemas/`, `agent/`, `backend/`, `frontend/`, `tools/`, `data/`, `memory/`)

## 0. 시작 전 (5분)

- [ ] 전원 레포 clone, 가상환경 생성, `pip install -r requirements.txt`
- [ ] `cp .env.example .env` 후 OPENAI_API_KEY 채우기
- [ ] `uvicorn backend.main:app --reload` 부팅 확인 (`http://localhost:8000/health` → 200)
- [ ] `pytest tests/test_smoke.py` 통과 확인

## 1. 슬라이스 분배 확정 (10분)

`plan.md` 분담 표를 루트 `CLAUDE.md` 1번에 옮기고 미정 항목 합의.

| 슬라이스 | 한 줄 책임 | 담당 |
|---|---|---|
| A | Flutter Web 프론트 (대시보드) | 노준영 |
| B | Chat UI + Agent 통신 프로토콜 | 박장우 |
| C | LangGraph Agent (prompt + graph + memory) | 이유준 |
| D | CRUD Tool + 시나리오 + 프롬프트 튜닝 | 박영준 |
| E | CRUD Tool + 시나리오 + 프롬프트 튜닝 | 신승민 |

**Tech Lead** (C 또는 D): ______ (매일 저녁 main 동작 확인 + Flutter↔FastAPI 통합 책임)
**D-E 도메인 분담**: ______ (예: D=calendar/workouts, E=health/scenarios)

## 2. LangGraph 페어 학습 (60분)

다 같이 화면 공유로 한 명이 따라치고, 나머지는 각자 노트북에서 동시 실행.

- [ ] LangGraph 공식 "Quickstart" 1회 완주 (간단한 ReAct Agent 1개 빌드)
- [ ] 우리 프로젝트의 `agent/graph.py`(`run_agent_stream`)에 어떻게 매핑될지 5분 토론
- [ ] StateGraph, Tool 노드, 체크포인터 컨셉 공유

## 3. 인터페이스 락 (10분)

세 가지 합의를 같은 자리에서 끝낸다:

- [ ] **Pydantic 모델** (`schemas/models.py`): 한 줄씩 같이 읽으며 빠진 필드 없는지 확인
- [ ] **Tool 시그니처** (D/E): `get_/create_/update_/delete_*` × calendar/health/workouts
- [ ] **REST 엔드포인트** (B/D/E): `/data/{calendar,health,workouts}` GET/POST/PATCH/DELETE
- [ ] **SSE 청크 포맷** (B/C): `ChatChunk.type` ∈ {text, tool_call, proposal, done, error}, type별 payload 표 (schemas/CLAUDE.md)
- [ ] **변경 사항이 있으면 지금 한 PR로 일괄 수정**. 이후엔 데일리 싱크에서만 변경.

## 4. 첫 PR 만들기 (10분)

각자 자기 슬라이스의 stub `NotImplementedError` / `501`을 **빈 함수가 더미를 반환**하도록 바꿔서 첫 PR을 올리고 머지한다. 5/4 EOD 체크리스트 통과가 목표.

- [ ] **A**: `cd frontend && flutter create .` 실행, 빈 Flutter Web 화면 1회 띄우기
- [ ] **B**: `frontend/lib/chat/` 디렉토리 + 채팅 위젯 골격 PR + `ChatChunk` payload 표 초안 (C와 합의)
- [ ] **C**: `agent/graph.py::run_agent_stream` 더미 청크 emit (이미 구현됨), 시스템 프롬프트 초안 (`agent/prompts.py`에 ReAct 3단계 강제 문구)
- [ ] **D**: `data/calendar.json` 더미 5건 + `tools.get_calendar` JSON 파싱 1차 구현
- [ ] **E**: `data/health.json` 더미 5건 + `tools.get_health` JSON 파싱 1차 구현

(D-E의 도메인 분담은 위 1번에서 정한 대로)

## 5. 운영 룰 확정 (5분)

- [ ] 데일리 싱크 시간: ______ (15분, 매일 진행)
- [ ] 공유 채널: 카톡/디스코드 _______
- [ ] GitHub 레포 URL: ______, main 보호 ON, 리뷰어 1명 이상 필수
- [ ] PR 리뷰 turnaround 약속: 영업시간 기준 _____시간 내

## 6. 5/4 EOD 체크 (각자)

- [ ] `schemas/models.py`가 main에 머지되어 있다 (`ChatRequest`, `ChatChunk` 포함)
- [ ] 내 슬라이스의 첫 PR이 main에 머지되어 있다
- [ ] `uvicorn backend.main:app --reload`로 부팅, `GET /health` 200 OK
- [ ] `pytest` 통과
- [ ] (A) `flutter run -d chrome`으로 Flutter Web 빈 화면 떠 있다

## 다음 마일스톤

- 5/5 EOD: 각 슬라이스가 더미 입력으로 단독 실행 가능
- 5/8 EOD: ★ 1차 통합, Flutter ↔ FastAPI ↔ Agent end-to-end 1회 성공
- 5/10 EOD: 코드 동결, 데모 시나리오 무사고 시연
