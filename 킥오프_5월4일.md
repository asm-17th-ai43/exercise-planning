# 5/4(월) 킥오프 어젠다 — 90분

> 목적: **인터페이스 락 + 슬라이스 분배 확정 + 모두 첫 PR을 main에 올리기**.
> 자료: `CLAUDE.md`, `개발계획_B안_균등분배.md`, 레포 스캐폴딩 (`schemas/`, `app/`, `agent/`, `tools/`, `data/`, `visuals/`)

## 0. 시작 전 (5분)

- [ ] 전원 레포 clone, 가상환경 생성, `pip install -r requirements.txt`
- [ ] `cp .env.example .env` 후 OPENAI_API_KEY 채우기
- [ ] `streamlit run app/main.py` 동작 확인 (Hello World 화면이 떠야 함)
- [ ] `pytest tests/test_smoke.py` 통과 확인

## 1. 슬라이스 분배 (15분)

CLAUDE.md 1번 표에 이름 채우기. 선호도 → 가위바위보 순. Tech Lead는 #3 또는 #4 담당이 겸임.

| # | 슬라이스 | 담당자 | 비고 |
|---|---|---|---|
| 1 | 캘린더 | _____ | |
| 2 | 건강 | _____ | |
| 3 | 운동기록 + 그래프 | _____ | Tech Lead 후보 |
| 4 | 추론·프롬프트·메모리 | _____ | Tech Lead 후보 |
| 5 | 시각화·UX | _____ | |

**Tech Lead**: ______ (매일 저녁 main 동작 확인 + 통합 책임)

## 2. LangGraph 페어 학습 (60분)

다 같이 화면 공유로 한 명이 따라치고, 나머지는 각자 노트북에서 동시 실행.

- [ ] LangGraph 공식 "Quickstart" 1회 완주 (간단한 ReAct Agent 1개 빌드)
- [ ] 우리 프로젝트의 `agent/graph.py`에 어떻게 매핑될지 5분 토론
- [ ] StateGraph, Tool 노드, 체크포인터 컨셉 공유

## 3. 인터페이스 락 (10분)

- [ ] `schemas/models.py` 같이 한 줄씩 읽으며 빠진 필드 없는지 확인
- [ ] Tool 시그니처 (`get_calendar/get_health/get_workouts`) 합의
- [ ] `run_agent` / `render_fatigue` 진입점 합의
- [ ] **변경 사항이 있으면 지금 한 PR로 일괄 수정**. 이후엔 데일리 싱크에서만 변경.

## 4. 첫 PR 만들기 (10분)

각자 자기 슬라이스의 stub `NotImplementedError`를 **빈 함수가 더미를 반환**하도록 바꿔서 첫 PR을 올리고 머지한다. 5/4 EOD 체크리스트 통과가 목표.

- [ ] #1: `get_calendar` → `data/calendar.json` 그대로 읽어 반환 (필터 없이 OK)
- [ ] #2: `get_health` → `data/health.json` 그대로 반환
- [ ] #3: `get_workouts` → `data/workouts.json` 그대로 반환
- [ ] #4: `agent/graph.py::run_agent`에 "ReAct로 일정·건강·운동기록 호출 후 더미 ScheduleProposal 반환" 골격 추가
- [ ] #5: `render_fatigue`가 단색 PNG라도 반환, `app/main.py`에 결과 표시

## 5. 운영 룰 확정 (5분)

- [ ] 데일리 싱크 시간: ______ (15분, 매일 진행)
- [ ] 공유 채널: 카톡/디스코드 _______
- [ ] GitHub 레포 URL: ______, main 보호 ON, 리뷰어 1명 이상 필수
- [ ] PR 리뷰 turnaround 약속: 영업시간 기준 _____시간 내

## 6. 5/4 EOD 체크 (각자)

- [ ] `schemas/models.py`가 main에 머지되어 있다
- [ ] 내 슬라이스의 첫 PR이 main에 머지되어 있다
- [ ] `streamlit run app/main.py`로 화면이 뜨고 채팅 입력 시 stub 응답이 나온다
- [ ] `pytest` 통과

## 다음 마일스톤

- 5/5 EOD: 각 슬라이스가 더미 입력으로 단독 실행 가능
- 5/8 EOD: ★ 1차 통합, end-to-end 1회 성공
- 5/10 EOD: 코드 동결, 데모 시나리오 무사고 시연
